spring:
  application:
    name: networking2

  datasource:
    url: jdbc:mysql://localhost:3306/cotato_assignment?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: choosungah
    password: choosungah03!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  profiles:
    active: dev