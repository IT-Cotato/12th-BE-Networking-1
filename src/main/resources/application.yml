spring:
  application:
    name: networking2
  datasource:
    url: jdbc:mysql://localhost:3307/12th-BE-Networking-1
    username: gimin
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
